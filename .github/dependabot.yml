version: 2
updates:
  # Enable version updates for Cargo dependencies
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    open-pull-requests-limit: 10
    reviewers:
      - "alexwoolford"
    labels:
      - "dependencies"
      - "rust"
    commit-message:
      prefix: "chore"
      include: "scope"
    # Group updates to reduce PR noise
    groups:
      patch:
        patterns:
          - "*"
        update-types:
          - "patch"
      minor:
        patterns:
          - "*"
        update-types:
          - "minor"
    # Ignore major version updates (review manually)
    ignore:
      - dependency-name: "*"
        update-types:
          - "version-update:semver-major"
      # Ignore hickory-resolver updates - 0.25 has breaking changes that require code updates
      # See: src/initialization/resolver.rs comments about 0.25 search domain issues
      - dependency-name: "hickory-resolver"
        update-types:
          - "version-update:semver-major"
          - "version-update:semver-minor"
          - "version-update:semver-patch"
      # Ignore idna updates - tldextract 0.6.0 requires idna 0.2.x, while other deps use idna 1.x
      # This creates a dual-version situation that Dependabot can't resolve automatically
      # Ignore until tldextract is updated to support idna 1.x
      - dependency-name: "idna"
        update-types:
          - "version-update:semver-major"
          - "version-update:semver-minor"
          - "version-update:semver-patch"
